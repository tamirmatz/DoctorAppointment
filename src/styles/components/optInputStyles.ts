import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
      flexDirection: 'row',
      alignItems: 'center',
      gap: 15,
      padding: 15
    },
    input: {
      width: 60,
      height: 70,
      borderWidth: 2,
      borderColor: '#C1C5C1',
      borderRadius: 8,
      textAlign: 'center',
      fontSize: 24,
      color: '#000',
      backgroundColor: '#fff'
    },
  });